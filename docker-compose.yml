version: "3.9"
services:
  postgres:
    image: postgres:16
    container_name: test_postgres
    ports:
      - "5438:5432" 
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
      POSTGRES_DB: testdb
